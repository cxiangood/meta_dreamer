# 面向顶会投稿的自动驾驶研究方案（2026 升级版）

## 1. 研究目标与当前瓶颈
当前 `submit_il_rl.sh` 对应的 IL+RL 训练可跑通，但存在两个核心问题：

1. 奖励函数过度依赖手工 shaping，且存在负奖励截断，容易导致 reward hacking 与错误对齐。
2. 主训练任务长期集中在单一路型（lane keeping），泛化与长尾交互能力不足。

本方案目标是将系统升级为可投稿顶会的研究框架：

1. 学到多模态、可校准、可解释的轨迹分布。
2. 在安全约束下提升闭环表现，而非仅提升 reward 分数。
3. 在多场景课程训练中验证泛化能力与长尾鲁棒性。

## 2. 为什么“原创新点不够”
仅“Flow Matching + Occupancy + RL 微调”确实已是主流方向，单独提出不足以成为强创新。要达到顶会竞争力，需要回答这三个更难的问题：

1. 如何避免手工 reward 设计主导结论，并给出可泛化的安全目标？
2. 如何系统地制造长尾交互，而不是依赖仿真器默认场景分布？
3. 如何让多模态规划输出具备风险可校准性，而非只看均值指标？

## 3. 升级后的核心方法（FlowDreamer-AD++）

### 3.1 方法总览
构建四阶段训练流程：

1. 阶段A：DAgger 预训练（保留你现有流程）。
2. 阶段B：世界模型条件流匹配（生成 K 模态轨迹）。
3. 阶段C：约束优化（CMDP）替代手工 reward 堆叠。
4. 阶段D：反事实场景课程（Counterfactual Curriculum）强化长尾能力。

### 3.2 模块 M1：约束奖励学习（解决“奖励粗糙”）
不再把所有目标揉进一个 reward 标量，而是拆为“任务收益 + 安全成本”：

1. 主收益 `r_task`：路线进度 + 到达奖励（稀疏且稳定）。
2. 成本 `c_safety`：碰撞、越界、压线、TTC 违约、舒适性超限。
3. 训练目标：`max E[r_task]`，约束 `E[c_safety] <= b`（拉格朗日优化）。

额外升级：使用你现有 `risk_trigger/use_expert` 事件作为隐式偏好信号，学习 reward/cost 权重（而非固定人工权重）。

### 3.3 模块 M2：反事实场景课程（解决“场景单一”）
基于世界模型 latent 与现有 MetaDrive 场景，生成困难但可解的反事实场景：

1. 从当前失败轨迹中抽取“临界状态”（near-crash/merge deadlock）。
2. 在该状态附近扰动对手车速度、相对位置、cut-in 时机与交通密度。
3. 用难度评分器控制课程节奏：从可解难例逐步升级到极限长尾。

这比简单增加 `num_scenarios` 更有研究价值：它是目标导向的困难样本生成。

### 3.4 模块 M3：风险可校准的多模态流匹配
多模态轨迹不仅输出均值，还输出可置信风险分位信息：

1. 每个模态输出 `(trajectory, risk_quantile)`。
2. 使用 conformal 校准保证风险分位与实际违规率匹配。
3. 决策时采用“约束满足下的最优模态选择”而非仅最大回报。

这一步用于建立“可证明更安全”的实证证据（校准曲线 + 违规覆盖率）。

## 4. 与现有工程的直接对接

### 4.1 奖励/成本接口改造
建议优先修改：

1. `dreamer/embodied/envs/metadrive_lane_keeping.py`
2. `dreamer/embodied/envs/metadrive_on_ramp.py`
3. `dreamer/embodied/envs/metadrive_lane_reduction.py`

改造内容：

1. 新增 `cost` 字段并输出分项成本（collision/offroad/lane_line/ttc/comfort）。
2. 去掉 `no_negative_reward` 的硬截断，避免掩盖危险前兆。
3. 保留 `reward` 作为任务收益，不再承载所有安全约束。

### 4.2 多场景训练入口
建议新增配置 `metadrive_multiscene_dagger`：

1. 训练分布：lane_keeping / lane_reduction / on_ramp 混合采样。
2. 课程权重：`[0.6, 0.3, 0.1] -> [0.3, 0.4, 0.3] -> [0.2, 0.3, 0.5]`。
3. 难度轴：交通密度、对手 aggressiveness、可见性/扰动强度。

### 4.3 参数映射（基于现有 `submit_il_rl.sh`）
新增参数组（示例）：

1. `--env.metadrive.cost_budget 0.12`
2. `--env.metadrive.lagrange_lr 1e-3`
3. `--env.metadrive.ttc_threshold 1.5`
4. `--env.metadrive.curriculum_stage 0/1/2`
5. `--env.metadrive.scenario_mix lane_keeping:0.6,lane_reduction:0.3,on_ramp:0.1`
6. `--env.metadrive.cf_augment_prob 0.3`

## 5. 论文级创新点

1. 约束奖励学习 + 干预偏好蒸馏：
把 DAgger 风险触发事件转化为偏好监督，自动学习安全成本权重，减少手工 reward 依赖。

2. 反事实课程生成（Counterfactual Curriculum）：
不是被动使用仿真器默认场景，而是围绕失败边界主动构造困难交互样本，系统提升长尾能力。

3. 风险可校准多模态流匹配：
将多模态轨迹生成与风险分位校准联合，提供“可解释、可校准、可约束”的部署决策机制。

4. 统一闭环证据链：
同时报告任务收益、安全成本、风险校准误差与实时性，避免“只刷单一分数”的论文弱点。

## 6. 数据与评测协议

### 6.1 数据
1. 主闭环：Bench2Drive + NAVSIM。
2. 迁移预训练：nuPlan + Waymo Motion。
3. 自建长尾：仅作补充和失败分析。

### 6.2 指标（必须同时报告）
1. 任务：success rate、route completion。
2. 安全：collision/offroad/lane violation/red-light。
3. 成本约束：平均 `c_safety`、超预算概率。
4. 校准：risk ECE、分位覆盖误差。
5. 效率：ms/step、FPS、显存。

### 6.3 关键消融
1. 无 CMDP（回退到手工 shaping）。
2. 无反事实课程（仅默认场景）。
3. 无风险校准（仅多模态轨迹）。
4. Diffusion vs Flow Matching。
5. 不同模态数 K 与时延权衡。

## 7. DiscoRL 与传统方法对比计划

### 7.1 可行性结论
1. 可以做：采用“DiscoRL 已发现更新规则迁移（transfer）”作为先进优化基线。
2. 不建议短期做：从零执行 DiscoRL 的规则发现（meta-discovery）成本过高。
3. 论文建议定位：`PPO（强传统） + 现有 AC（弱传统） + Dreamer 主线 + DiscoRL-transfer` 四组对比。

### 7.2 公平对比协议
1. 同训练预算：相同环境步数、相同 wall-clock、相同 GPU 配置。
2. 同数据分布：lane_keeping / lane_reduction / on_ramp 的一致课程配置。
3. 同评测指标：success、collision、offroad、route completion、comfort、时延/FPS。
4. 同统计方式：至少 5 seeds，报告均值与方差，并给出学习曲线。

### 7.3 实施顺序
1. 先补标准 PPO 基线（clip ratio + old logprob + GAE + minibatch epochs）。
2. 保留当前 AC 作为基础传统基线。
3. 再接入 DiscoRL-transfer 更新规则并在同配置下复现实验。
4. 产出对比图：样本效率曲线、终点性能柱状图、成本-收益帕累托图。

## 8. 无人工数据条件下的 VLM 指令执行方案

### 8.1 可行性结论
在没有自有人类演示数据的情况下，可以训练一个“简单指令执行”VLM，但需要将目标限定为：

1. 指令类型：车道保持、跟车、变道、汇入、减速避让等短指令。
2. 执行层级：中低速闭环驾驶中的短时行为控制，不追求开放域复杂对话。
3. 数据来源：仅使用公开自动驾驶与视频-文本数据，不依赖人工新标注。

### 8.2 数据构成（公开数据）
1. 驾驶轨迹与场景数据：nuPlan、Waymo Motion、NAVSIM/Bench2Drive。
2. 视频-语言预训练数据：通用 VLM 公开语义数据（用于语言对齐底座）。
3. 自动伪标注数据：利用规则模板从轨迹与场景状态自动生成指令文本。

### 8.3 关键方法（不依赖人工标注）
1. 指令伪标注器：
从状态机事件自动生成文本，如“前方慢车，向左变道并保持安全车距”。
2. 轨迹-指令对齐训练：
用 `(图像/状态, 指令) -> 轨迹分布` 做监督，先行为克隆后闭环微调。
3. 偏好蒸馏：
用 `risk_trigger/use_expert` 事件做正负偏好对，训练“更安全的指令执行偏好”。
4. 失败过滤：
对伪标签质量做筛选，去除冲突指令和不一致轨迹片段。

### 8.4 MVP 目标（4-6 周）
1. 支持 20-40 条模板化驾驶指令。
2. 在多场景闭环中实现可观测的指令跟随成功率提升。
3. 保持安全成本不劣于无指令策略基线。

### 8.5 风险与边界
1. 伪标签噪声高：需要置信度过滤与 curriculum。
2. 指令覆盖窄：先做封闭语义集合，再逐步扩展词汇。
3. 语言幻觉问题：部署时强制“可执行指令集合约束”，避免自由生成。

## 9. 8 周执行计划（按优先级）

### 周1-2：奖励/成本重构
1. 环境输出 `reward + cost` 与分项日志。
2. 接入拉格朗日约束训练。

### 周3-4：多场景混合训练
1. 新建 `metadrive_multiscene_dagger`。
2. 课程调度器与场景混采跑通。

### 周5：反事实样本生成
1. 实现失败边界状态采样。
2. 实现对手车参数扰动与难度打分。

### 周6：风险可校准多模态规划
1. 接入风险分位头。
2. 接入 conformal 校准与阈值策略。

### 周7：大规模实验
1. 主结果与关键消融。
2. 场景分组报表与失败案例库。

### 周8：论文打磨
1. 方法图、训练流程图、校准图、成本-收益帕累托图。
2. 完成主文 + 附录 + 复现清单。

## 10. 成功标准（Go/No-Go）
进入投稿冲刺前至少满足：

1. 相对当前基线：碰撞率下降 >= 20%，成功率提升 >= 8%。
2. 在成本预算约束下仍保持任务收益提升。
3. 风险分位覆盖误差显著优于未校准基线。
4. 推理时延满足闭环实时需求（给出硬件条件下的实测 FPS）。

## 11. 风险与备选
1. 约束优化不稳定：先固定拉格朗日系数，再逐步自适应。
2. 反事实样本过难导致训练崩溃：限制扰动半径并做可解性过滤。
3. 校准造成性能回退：采用温度缩放 + 阈值搜索折中。

---
该升级版方案的核心是：把“会开车”问题升级为“在安全约束下、对长尾可泛化且风险可校准地开车”。这比常规 flow planner 更具新意，也更符合顶会审稿偏好。

## TODO（执行状态）
- [x] 完成研究方案文档升级（奖励粗糙/场景单一/创新点增强/评测协议）。
- [x] 增加 DiscoRL vs PPO 对比计划到方案文档。
- [x] 增加“无人工数据下 VLM 指令执行”方案到文档。
- [x] 实现 NAVSIM v1 离线指令训练脚本：`dreamer/tools/navsim_instruction_v1.py`。
- [x] 增加一键运行脚本：`run_navsim_instruction_v1.sh`。
- [x] 实现多实验自动统计工具：`dreamer/tools/analyze_experiments.py`（CSV/JSON/Markdown/HTML）。
- [x] 支持任务结束自动触发分析：`submit_il_rl.sh` 训练后自动生成分析报告。
- [ ] 安装训练依赖（当前缺少 `flax`，未通过本机真实训练）。
- [ ] 对接真实 NAVSIM 数据格式并跑首轮训练（非 synthetic）。
- [ ] 增加 PPO/CMDP 微调脚本并接入指令条件策略。
- [ ] 增加 GRPO 风格微调对比实验。
- [ ] 输出首版实验报告（success/collision/offroad/成本约束/时延）。
